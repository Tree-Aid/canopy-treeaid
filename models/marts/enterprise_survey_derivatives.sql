{# Requires Jane to do some more clean up on the db by replacing manually uploaded tables with repeat groups with connector tables 

{{survey_type_table('Enterprise survey', repeat='derivatives')}} 
 
#} 

select 1